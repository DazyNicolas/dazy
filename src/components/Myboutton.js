import React from 'react';

const Myboutton = () => {
    return (
        <div>
            <button className='mybutton'>Commencer</button>
        </div>
    );
};

export default Myboutton;