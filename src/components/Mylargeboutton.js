import React from 'react';

const Myboutton = () => {
    return (
        <div>
            <button className='mylargeboutton'>Commencer</button>
        </div>
    );
};

export default Myboutton;